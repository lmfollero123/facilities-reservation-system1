<?php
/**
 * Copy to config/database.php (optional — repo includes database.php with .env overrides).
 * Prefer setting DB_* in .env only; do not commit real passwords.
 */
const DB_HOST = 'localhost';
const DB_NAME = 'facilities_reservation';
const DB_USER = 'root';
const DB_PASS = '';
