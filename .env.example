APP_ENV=local

# Captcha (Cloudflare Turnstile) - free anti-spam
# 1) Create keys at https://dash.cloudflare.com/turnstile
# 2) Add hostnames on the widget: cprf.infragovservices.com (prod) AND lgu.test / localhost (local)
# 3) Local-only test keys (always pass): site 1x00000000000000000000AA / secret 1x00000000000000000000AA
# Production: prefer ~/private/cprf.env (overrides .env on cPanel)
CAPTCHA_ENABLED=false
# Login page: show Turnstile only after this many failed attempts (per email or IP), not on every visit
CAPTCHA_LOGIN_AFTER_FAILED=2
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Database
DB_HOST=localhost
DB_NAME=facilities_reservation
DB_USER=root
DB_PASS=

# Mail / SMTP
MAIL_SMTP_ENABLED=true
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_ENCRYPTION=tls
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_EMAIL=no-reply@example.com
MAIL_FROM_NAME=LGU Facilities
# Optional anti-spam mail guard (limits outbound SMTP)
MAIL_RATE_LIMIT_GLOBAL_MAX=80
MAIL_RATE_LIMIT_GLOBAL_WINDOW=600
MAIL_RATE_LIMIT_PER_RECIPIENT_MAX=5
MAIL_RATE_LIMIT_PER_RECIPIENT_WINDOW=600
MAIL_RATE_LIMIT_PER_IP_MAX=20
MAIL_RATE_LIMIT_PER_IP_WINDOW=600

# Booking reminders (cron: scripts/send_booking_reminders.php)
BOOKING_REMINDER_HOURS_BEFORE=24

# Resident booking limits (Staff/Admin bypass). Walk-in bookings apply limits to the resident.
BOOKING_MAX_PER_DAY=1
BOOKING_MAX_PER_WEEK=3
BOOKING_MAX_PER_MONTH=8
BOOKING_MAX_PER_YEAR=96
BOOKING_MAX_UPCOMING_ACTIVE=2
BOOKING_ADVANCE_MAX_DAYS=60

# Staff pending-approval digest (cron: scripts/send_staff_pending_digest.php)
STAFF_PENDING_DIGEST_ENABLED=true

# Gemini AI
GEMINI_API_KEY=
# Auto-publish public announcements when CIMM schedules facility maintenance (uses Gemini for copy; facility photo attached)
# Set to false to disable. Default: enabled when GEMINI_API_KEY is set.
CIMM_AUTO_ANNOUNCEMENTS=true
# Auto-publish when CPRF staff add manual blackout dates (single day or range)
CPRF_AUTO_ANNOUNCEMENTS=true

# Geocoding providers (optional)
MAPBOX_ACCESS_TOKEN=
GOOGLE_MAPS_API_KEY=

# CIMM maintenance integration (optional)
CIMM_API_KEY=
# Keys shared with CIMM for two-way integration (must match CIMM server config)
FACILITIES_API_KEY=FACILITIES_SECURE_KEY_2025
# Inbound webhook auth for POST /api/integrations/* (X-API-Key or Bearer). Falls back to FACILITIES_API_KEY if empty.
INTEGRATIONS_INBOUND_KEY=
LGU_WEBHOOK_KEY=LGU_TO_FACILITIES_KEY_2025

# UMAN utilities management integration (optional)
UMAN_API_URL=https://uman.infragovservices.com
UMAN_API_KEY=UMAN_SECURE_KEY_2025

# SMS (IPROG) — https://www.iprogsms.com/api/v1/documentation
SMS_ENABLED=true
SMS_DRIVER=iprogsms
SMS_DEFAULT_RECIPIENT=
IPROG_API_TOKEN=
IPROG_ENDPOINT=https://www.iprogsms.com/api/v1/sms_messages
# Other drivers: log | email_gateway | philsms (legacy)
# --- Email-to-SMS gateway (free via your Gmail SMTP; best for Globe/TM) ---
# SMS_DRIVER=email_gateway
# SMS_EMAIL_GLOBE_DOMAIN=messaging.globe.com.ph
# SMS_EMAIL_SMART_DOMAIN=messaging.smart.com.ph
# SMS_EMAIL_DEFAULT_NETWORK=globe
# SMS_EMAIL_SUBJECT=SMS
# --- Demo log only (no real SMS) ---
# SMS_DRIVER=log
# SMS_LOG_PATH=

APP_ENV=local

# Production/public site URL (used in emails, PayMongo redirects). Ignored when APP_ENV=local —
# local dev auto-detects from your browser URL (e.g. http://localhost/...).
APP_URL=https://cprf.infragovservices.com

# Optional: force a specific base URL in local dev (e.g. ngrok for testing email links on phone)
# APP_URL_LOCAL=https://your-subdomain.ngrok-free.app

# AI / ML — PHP calls Python for predictions (Windows: use full path if tests show error 9009)
# FRS_PYTHON=C:\Users\QC SDO\AppData\Local\Programs\Python\Python310\python.exe
# PYTHON_EXECUTABLE=

# Dev-only UI: AI Model Lab + Book a Facility demo scenarios (hidden by default)
# FRS_AI_DEV_TOOLS=true

# Payments
PAYMENTS_ENABLED=true
REQUIRE_PAYMENT_FOR_RESERVATIONS=true
PAYMENT_WINDOW_MINUTES=60
PAYMENT_CURRENCY=PHP
PAYMONGO_SECRET_KEY=
PAYMONGO_WEBHOOK_SECRET=
PAYMONGO_API_VERSION=v2
PAYMONGO_LINKS_ENDPOINT=https://api.paymongo.com/v1/links
PAYMONGO_DESCRIPTION_PREFIX=LGU Culiat Reservation
